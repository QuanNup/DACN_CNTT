import React from 'react';
import StoreManagerPage from '../../ui/dashboard_admin/pages-admin-dashboard/page.store-manager';
import { auth } from '@/auth';

const StoreManager = async () => {
    return (
        <>
            <StoreManagerPage />
        </>
    );
};

export default StoreManager;